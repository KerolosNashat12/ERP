/**
 * Strings for the shop-detail screens, kept out of `i18n.js` so the two halves
 * of the console can be worked on without editing the same file. Merged in by
 * `i18n.js` at load; a key here never overwrites one there.
 */
export const detailStrings = {
  en: {},
  ar: {},
};

export default detailStrings;
